# Minimal Instance CR — smallest viable standalone Milvus.
apiVersion: core.openeverest.io/v1alpha1
kind: Instance
metadata:
  name: milvus-standalone-simple
spec:
  providerRef:
    name: milvus
  components:
    standalone:
      type: milvus
      replicas: 1
